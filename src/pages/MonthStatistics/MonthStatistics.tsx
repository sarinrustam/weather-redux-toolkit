import React from 'react';

import styles from './MonthStatistics.module.scss';

interface Props {
    
}

const MonthStatistics = (props: Props) => {
  return (
    <div className={styles.root}>MonthStatistics</div>
  )
}

export default MonthStatistics;
